import type{User}from'firebase/auth';
export async function uploadPostImage(user:User,file:File){const token=await user.getIdToken(),ticket=await fetch('/api/media/presign',{method:'POST',headers:{'content-type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({name:file.name,type:file.type,size:file.size})}),data=await ticket.json();if(!ticket.ok)throw new Error(data.error||'Could not prepare image upload.');const uploaded=await fetch(data.uploadUrl,{method:'PUT',headers:{'Content-Type':file.type},body:file});if(!uploaded.ok)throw new Error('Image upload failed. Check the R2 CORS policy and try again.');return String(data.publicUrl)}
