'use client';
import{useEffect,useState}from'react';import Link from'next/link';
export function CookieBanner(){const[show,setShow]=useState(false);useEffect(()=>setShow(!localStorage.getItem('ex-cookie-choice')),[]);if(!show)return null;function choose(value:'necessary'|'all'){localStorage.setItem('ex-cookie-choice',value);setShow(false)}return <aside role="dialog" aria-label="Cookie choices" className="fixed inset-x-3 bottom-3 z-[70] mx-auto max-w-3xl rounded-[26px] border border-white/50 bg-white/75 p-5 shadow-2xl backdrop-blur-3xl dark:bg-[#111521]/85"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><p className="flex-1 text-sm leading-6"><b className="block text-base">Your privacy choices</b>VAYXO uses essential storage for authentication, security, and your preference. Optional analytics remain off unless you accept them. <Link href="/legal/cookies" className="font-semibold text-primary">Cookie Policy</Link></p><div className="flex gap-2"><button onClick={()=>choose('necessary')} className="rounded-full border px-4 py-2 text-sm font-bold">Necessary only</button><button onClick={()=>choose('all')} className="rounded-full bg-primary px-4 py-2 text-sm font-bold text-white">Accept all</button></div></div></aside>}
