'use client';
import {useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {Send,Sparkles,X} from 'lucide-react';
export function AiDock(){const[open,setOpen]=useState(false),[prompt,setPrompt]=useState(''),[answer,setAnswer]=useState(''),[busy,setBusy]=useState(false);async function ask(e:React.FormEvent){e.preventDefault();if(!prompt.trim())return;setBusy(true);setAnswer('');try{const r=await fetch('/api/ai',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({prompt,context:location.pathname})});const j=await r.json();if(!r.ok)throw new Error(j.error);setAnswer(j.text)}catch(e){setAnswer(e instanceof Error?e.message:'AI is unavailable')}finally{setBusy(false)}}return <><button onClick={()=>setOpen(true)} aria-label="Open VAYXO AI" className="fixed bottom-20 right-4 z-40 flex items-center gap-2 rounded-full border border-white/40 bg-white/55 px-4 py-3 font-bold text-black shadow-[0_12px_40px_#00000020] backdrop-blur-2xl dark:bg-[#151a28]/70 dark:text-white md:bottom-6"><Sparkles className="size-5"/>VAYXO AI</button><AnimatePresence>{open&&<motion.aside initial={{opacity:0,y:30,scale:.96}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20}} className="fixed bottom-4 right-4 z-50 flex h-[min(560px,85vh)] w-[min(390px,calc(100vw-24px))] flex-col overflow-hidden rounded-[30px] border border-white/50 bg-white/70 shadow-2xl backdrop-blur-3xl dark:bg-[#111521]/80"><header className="flex items-center gap-3 border-b border-white/40 p-4"><span className="grid size-9 place-items-center rounded-2xl bg-black text-white dark:bg-white dark:text-black"><Sparkles className="size-4"/></span><div><b>VAYXO AI</b><small className="block text-muted-foreground">Powered by Groq</small></div><button onClick={()=>setOpen(false)} className="ml-auto rounded-full p-2 hover:bg-white/50"><X className="size-5"/></button></header><div className="flex-1 overflow-auto p-5"><p className="rounded-2xl bg-white/60 p-4 text-sm dark:bg-white/5">Ask me to rewrite a post, suggest replies, summarize a thread, explore a topic, or draft a message.</p>{answer&&<p className="mt-4 whitespace-pre-wrap rounded-2xl bg-primary p-4 text-sm text-white">{answer}</p>}{busy&&<p className="mt-4 animate-pulse text-sm text-muted-foreground">Thinking at Groq speed…</p>}</div><form onSubmit={ask} className="flex gap-2 border-t border-white/40 p-3"><input value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Ask VAYXO AI…" className="min-w-0 flex-1 rounded-full bg-white/60 px-4 outline-none dark:bg-white/5"/><button className="rounded-full bg-primary p-3 text-white"><Send className="size-4"/></button></form></motion.aside>}</AnimatePresence></>}
