export function IosSpinner({size=20,label='Loading',className=''}:{size?:number;label?:string;className?:string}){return <span role="status" aria-label={label} className={`vayrox-ios-spinner ${className}`} style={{width:size,height:size}}>{Array.from({length:12}).map((_,i)=><i key={i} style={{transform:`rotate(${i*30}deg) translateY(-38%)`,animationDelay:`${-1.1+i*.1}s`}}/>)}</span>}
