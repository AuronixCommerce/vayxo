'use client';
import{AnimatePresence,motion}from'framer-motion';
import{Check,X}from'lucide-react';
export function ConfirmDialog({open,title,description,confirmLabel='Confirm',cancelLabel='Cancel',danger=false,busy=false,onConfirm,onCancel}:{open:boolean;title:string;description?:string;confirmLabel?:string;cancelLabel?:string;danger?:boolean;busy?:boolean;onConfirm:()=>void|Promise<void>;onCancel:()=>void}){return <AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[100] grid place-items-end bg-black/45 p-3 backdrop-blur-sm sm:place-items-center" onMouseDown={onCancel}><motion.div initial={{y:24,scale:.98}} animate={{y:0,scale:1}} exit={{y:18,opacity:0}} transition={{type:'spring',damping:28,stiffness:330}} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-md rounded-[30px] border bg-background p-5 shadow-2xl"><div className="flex items-start gap-3"><div className="min-w-0 flex-1"><h2 className="text-xl font-black">{title}</h2>{description&&<p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p>}</div><button onClick={onCancel} className="rounded-full p-2 hover:bg-secondary" aria-label="Close"><X className="size-5"/></button></div><div className="mt-6 grid grid-cols-2 gap-2"><button onClick={onCancel} disabled={busy} className="rounded-full border px-4 py-3 text-sm font-bold hover:bg-secondary disabled:opacity-50">{cancelLabel}</button><button onClick={()=>void onConfirm()} disabled={busy} className={`rounded-full px-4 py-3 text-sm font-black text-white disabled:opacity-50 ${danger?'bg-red-600':'bg-foreground text-background'}`}>{busy?'Working…':confirmLabel}</button></div></motion.div></motion.div>}</AnimatePresence>}
type Choice<T extends string>={value:T;label:string;description?:string;disabled?:boolean};
export function ChoiceSheet<T extends string>({open,title,value,options,onChange,onClose}:{open:boolean;title:string;value:T;options:Choice<T>[];onChange:(value:T)=>void;onClose:()=>void}){return <AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[100] flex items-end justify-center bg-black/40 p-3 backdrop-blur-sm sm:items-center" onMouseDown={onClose}><motion.div initial={{y:28,scale:.99}} animate={{y:0,scale:1}} exit={{y:24,opacity:0}} transition={{type:'spring',damping:30,stiffness:340}} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-md overflow-hidden rounded-[30px] border bg-background shadow-2xl"><div className="flex items-center border-b px-5 py-4"><h2 className="text-lg font-black">{title}</h2><button onClick={onClose} className="ml-auto rounded-full p-2 hover:bg-secondary"><X className="size-5"/></button></div><div className="p-2">{options.map(option=><button key={option.value} disabled={option.disabled} onClick={()=>{onChange(option.value);onClose()}} className="flex w-full items-center gap-3 rounded-2xl px-4 py-3 text-left hover:bg-secondary disabled:opacity-40"><span className={`grid size-6 place-items-center rounded-full border ${value===option.value?'bg-foreground text-background':''}`}>{value===option.value&&<Check className="size-3.5"/>}</span><span className="min-w-0 flex-1"><b className="block text-sm">{option.label}</b>{option.description&&<small className="mt-0.5 block text-muted-foreground">{option.description}</small>}</span></button>)}</div></motion.div></motion.div>}</AnimatePresence>}
export function PageSkeleton({rows=5}:{rows?:number}){return <div className="animate-pulse">{Array.from({length:rows}).map((_,i)=><div key={i} className="flex gap-3 border-b p-4"><div className="size-11 rounded-full bg-secondary"/><div className="flex-1 space-y-3"><div className="h-4 w-2/5 rounded bg-secondary"/><div className="h-4 w-11/12 rounded bg-secondary"/><div className="h-4 w-8/12 rounded bg-secondary"/></div></div>)}</div>}
