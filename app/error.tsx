'use client';
import{useEffect}from'react';import Link from'next/link';import{AlertTriangle,Home,RefreshCw}from'lucide-react';import{motion}from'framer-motion';
export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error(error)},[error]);return <main className="grid min-h-screen place-items-center p-5"><motion.section initial={{opacity:0,y:16,scale:.98}} animate={{opacity:1,y:0,scale:1}} className="w-full max-w-md rounded-[34px] border bg-background/75 p-8 text-center shadow-2xl backdrop-blur-3xl"><motion.span initial={{rotate:-8,scale:.9}} animate={{rotate:0,scale:1}} className="mx-auto grid size-16 place-items-center rounded-3xl bg-secondary"><AlertTriangle className="size-7"/></motion.span><h1 className="mt-6 text-3xl font-black">Something went wrong</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">VAYROX hit an unexpected error on this screen. Your account and saved data have not been intentionally changed.</p><div className="mt-7 grid gap-3 sm:grid-cols-2"><button onClick={reset} className="inline-flex items-center justify-center gap-2 rounded-full bg-foreground px-5 py-3 text-sm font-bold text-background"><RefreshCw className="size-4"/>Try again</button><Link href="/home" className="inline-flex items-center justify-center gap-2 rounded-full border px-5 py-3 text-sm font-bold hover:bg-secondary"><Home className="size-4"/>Go home</Link></div>{error.digest&&<p className="mt-5 text-xs text-muted-foreground">Reference: {error.digest}</p>}</motion.section></main>}
