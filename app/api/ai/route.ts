import {NextResponse} from 'next/server';
const hits=new Map<string,{count:number;reset:number}>();
export async function POST(req:Request){const ip=req.headers.get('x-forwarded-for')?.split(',')[0]||'local',now=Date.now(),slot=hits.get(ip);if(slot&&slot.reset>now&&slot.count>=20)return NextResponse.json({error:'AI rate limit reached. Try again shortly.'},{status:429});hits.set(ip,{count:slot&&slot.reset>now?slot.count+1:1,reset:slot&&slot.reset>now?slot.reset:now+60_000});const key=process.env.GROQ_API_KEY;if(!key)return NextResponse.json({error:'VAYXO AI is not configured yet.'},{status:503});const body=await req.json().catch(()=>({})),prompt=String(body.prompt||'').slice(0,4000);if(!prompt)return NextResponse.json({error:'Prompt is required.'},{status:400});const response=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.GROQ_MODEL||'openai/gpt-oss-120b',temperature:.7,max_completion_tokens:1000,messages:[{role:'system',content:'You are VAYXO AI. Be fast, safe, warm, and useful. Help write posts, text stories, replies, summaries, profiles, support answers, and messages. Never claim to have published anything.'},{role:'user',content:`Current area: ${String(body.context||'unknown')}\n\n${prompt}`}]})});if(!response.ok)return NextResponse.json({error:'VAYXO AI request failed.'},{status:502});const data=await response.json();return NextResponse.json({text:data.choices?.[0]?.message?.content||'No response generated.'})}
